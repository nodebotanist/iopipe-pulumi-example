name: iopipe-pulumi-example
description: 
runtime: nodejs
template:
  description: A minimal AWS JavaScript program
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
